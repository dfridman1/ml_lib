#!/usr/bin/env python
from setuptools import setup, find_packages

setup(
    name='ml_lib',
    version='0.0.1',
    description='A Machine Learning Library',
    url='https://github.com/dfridman1/ml_lib',
    packages=find_packages(include=["ml_lib"]),
    install_requires=[
    ],
)
